@props([])

{{--
    The viewport shell is intentionally empty in this implementation.
    Content panels are self-contained: they teleport to body and position
    themselves directly below their trigger via x-ui-anchor. Including this
    component in a template is a no-op and will not produce visible output.
--}}
